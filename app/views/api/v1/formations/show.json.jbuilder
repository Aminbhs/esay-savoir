json.extract! @formation, :id, :nom, :programme, :date_debut, :date_fin, :nombre_place_total, :nombre_place_restante
# json.comments @client.comments do |comment|
#   json.extract! comment, :id, :content
# end
